import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  password: {
    marginHorizontal: 20,
    position: "relative"
  },

  checkbox: {
    position: "absolute",
    top: -20,
    right: -10,
    marginTop: 5
  }
})
